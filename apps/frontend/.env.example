# ─── StarkDCA Frontend — Environment Variables ───────────────────────
# Copy to .env and fill in values. All vars are PUBLIC (embedded in bundle).
# For production, set these in Vercel project settings.
# ─────────────────────────────────────────────────────────────────────

# REQUIRED | API base URL (backend)
# Local:  http://localhost:4000/api (proxied via Vite in dev)
# Prod:   https://YOUR-API-DOMAIN.onrender.com/api
VITE_API_URL=http://localhost:4000/api

# REQUIRED | Starknet chain
# SN_SEPOLIA for testnet, SN_MAIN for mainnet
VITE_STARKNET_CHAIN_ID=SN_SEPOLIA

# REQUIRED | DCA contract address on Starknet
VITE_DCA_CONTRACT_ADDRESS=0x_YOUR_CONTRACT_ADDRESS
